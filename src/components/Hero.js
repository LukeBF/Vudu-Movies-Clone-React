import React from 'react'

const Hero = (props) => {
    return (
        <>
            <div style={{backgroundImage:`url("${props.bannerImage}")`}} >


            </div>
            
            {/* Title */}
            {/* div with 2 buttons (optional)*/}
            {/* Description */}
        </>
    )
}

export default Hero
