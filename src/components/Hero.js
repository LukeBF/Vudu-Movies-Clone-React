import React from 'react'

const Hero = (props) => {
    return (
        <div style={{backgroundImage:`url("${props.image}")`}}>
            {/* Title */}
            {/* div with 2 buttons (optional)*/}
            {/* Description */}
        </div>
    )
}

export default Hero
